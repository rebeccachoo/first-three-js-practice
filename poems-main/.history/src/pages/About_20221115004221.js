import React from "react";
import Box from "@mui/material/Box";

const About = () => {
  return (
    <Box
      sx={{ display: "flex", alignItems: "center", justifyContent: "center" }}
    >
      This website is built with React.js and Material UI.
    </Box>
  );
};

export default About;
